export type AsideType = {
  name: string;
  pathname: string;
};

export type ExperienceType = {
  title: string;
  structure: string;
  periode: string;
  city: string;
  role: string[];
  techs: string[];
};

export type EducationType = {
  title: string;
  school: string;
  date: string;
};

export type ProjectType = {
  name: string;
  pathname: string;
};
